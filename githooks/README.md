# Simple Post Merge script

## Install
`$ run install.sh to copy post-merge hook to local git repo`

## Run
Repo will now automatically run `pod update`, and other additional processing in post-merge script
after any merge